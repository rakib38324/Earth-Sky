import background from '../../assets/nasa3.png'

const Education = () => {
    return (
        <div style={{ background: `url(${background})` }}
            className="relative h-screen !bg-no-repeat !bg-center !bg-cover "
        >
            {/* <div className="absolute top-0 left-0 w-full h-full bg-gradient-to-b from-slate-400 to-slate-100 opacity-20"></div> */}

            <p>Education lorem500</p>
        </div>
    );
};

export default Education;